---
phase: "01-项目基础设施"
plan: "04"
type: "execute"
wave: 2
depends_on: ["01-02"]
files_modified:
  - "src/facevidechange/logging_.py"
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "日志包含时间戳、模块名、日志级别"
    - "日志输出到 stdout"
    - "详细日志记录启动、预热、运行时状态"
  artifacts:
    - path: "src/facevidechange/logging_.py"
      provides: "日志系统"
---

<objective>
实现日志系统：verbose 模式、Rich handler、时间戳/模块/级别。

Purpose: 提供可配置的详细日志记录
Output: logging_.py 模块
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-项目基础设施/01-CONTEXT.md
@.planning/phases/01-项目基础设施/01-RESEARCH.md
@.planning/phases/01-项目基础设施/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
<name>Task 1: 实现日志系统</name>
<files>src/facevidechange/logging_.py</files>
<read_first>src/facevidechange/logging_.py (new file)</read_first>
<action>
创建 `src/facevidechange/logging_.py`，实现详细日志系统。

**设计要求 (per D-11, D-12, D-13, D-14):**

```python
from __future__ import annotations
import sys
import logging
from pathlib import Path
from typing import Optional
from rich.console import Console
from rich.logging import RichHandler
from rich.theme import Theme

# Rich 主题
custom_theme = Theme({
    "info": "cyan",
    "warning": "yellow",
    "error": "bold red",
    "debug": "dim",
})

# 全局 console 实例
_console = Console(stderr=True)

# 全局日志级别
_current_level = "INFO"

def setup_logging(
    level: str = "INFO",
    log_file: Optional[Path] = None,
    verbose: bool = True,
) -> None:
    """
    设置日志系统。
    
    Per D-11: 详细日志 — 启动、预热、运行时全部记录
    Per D-12: 日志输出到 stdout
    Per D-13: 日志分级: DEBUG / INFO / WARNING / ERROR
    Per D-14: 日志包含时间戳、模块名、日志级别
    """
    global _current_level
    _current_level = level.upper()
    
    handlers: list[logging.Handler] = []
    
    # Rich handler for stdout (colored, structured)
    rich_handler = RichHandler(
        console=_console,
        rich_tracebacks=True,
        tracebacks_show_locals=False,
        show_time=True,
        show_path=False,
        markup=True,
    )
    rich_handler.setFormatter(
        logging.Formatter(
            fmt="%(message)s",
            datefmt="[%X]",
        )
    )
    handlers.append(rich_handler)
    
    # File handler (if specified)
    if log_file:
        file_handler = logging.FileHandler(log_file, encoding="utf-8")
        file_handler.setFormatter(
            logging.Formatter(
                fmt="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
                datefmt="%Y-%m-%d %H:%M:%S",
            )
        )
        handlers.append(file_handler)
    
    # Configure root logger
    logging.basicConfig(
        level=getattr(logging, _current_level),
        format="%(message)s",
        datefmt="[%X]",
        handlers=handlers,
        force=True,
    )
    
    # Set specific levels for noisy libraries
    logging.getLogger("PIL").setLevel(logging.WARNING)
    logging.getLogger("matplotlib").setLevel(logging.WARNING)
    logging.getLogger("urllib3").setLevel(logging.WARNING)

def get_logger(name: str) -> logging.Logger:
    """获取带模块名前缀的 logger。"""
    return logging.getLogger(f"facevidechange.{name}")

class LogCapture:
    """上下文管理器，捕获日志输出用于测试。"""
    def __init__(self, logger_name: str = "facevidechange"):
        self.logger_name = logger_name
        self.records: list[logging.LogRecord] = []
        self.handler: Optional[logging.Handler] = None
    
    def __enter__(self):
        self.handler = logging.Handler()
        self.handler.setLevel(logging.DEBUG)
        self.handler.emit = lambda record: self.records.append(record)
        logging.getLogger(self.logger_name).addHandler(self.handler)
        return self
    
    def __exit__(self, *args):
        if self.handler:
            logging.getLogger(self.logger_name).removeHandler(self.handler)

# Convenience logger for startup, warmup, runtime stages
def log_stage(stage: str, message: str, level: str = "INFO") -> None:
    """记录启动/预热/运行时阶段日志。"""
    logger = get_logger(stage)
    getattr(logger, level.lower())(message)
```

关键点：
- 使用 `RichHandler` 提供彩色输出
- 日志格式: `[时间] [级别] 模块: 消息`
- 支持文件输出
- `get_logger(name)` 返回带模块前缀的 logger
- `log_stage()` 用于阶段日志
</action>
<verify>
<automated>python -c "from facevidechange.logging_ import setup_logging, get_logger; setup_logging('INFO'); log = get_logger('cli'); log.info('test'); print('OK')"</automated>
</verify>
<done>日志系统可配置，日志包含时间戳/模块名/级别</done>
</task>

<task type="auto">
<name>Task 2: 验证日志格式</name>
<files>src/facevidechange/logging_.py</files>
<read_first>src/facevidechange/logging_.py (existing)</read_first>
<action>
验证日志格式符合要求：
1. 启动日志示例：
   - `[12:34:56] INFO | facevidechange.startup: FaceVideoChange v0.1.0 starting...`
   - `[12:34:56] INFO | facevidechange.warmup: Loading face detector...`
2. 运行时日志示例：
   - `[12:34:56] INFO | facevidechange.runtime: FPS: 30, GPU: 65%`
3. 错误日志示例：
   - `[12:34:56] ERROR | facevidechange.pipeline: Frame processing failed`

测试脚本：
```python
from facevidechange.logging_ import setup_logging, get_logger

setup_logging("INFO")
startup_log = get_logger("startup")
warmup_log = get_logger("warmup")
runtime_log = get_logger("runtime")

startup_log.info("FaceVideoChange v0.1.0 starting...")
warmup_log.info("Loading face detector...")
runtime_log.info("FPS: 30, GPU: 65%")
```

确保日志格式包含时间戳、模块名和级别。
</action>
<verify>
<automated>python -c "
from facevidechange.logging_ import setup_logging, get_logger
setup_logging('DEBUG')
log = get_logger('test')
log.debug('debug message')
log.info('info message')
log.warning('warning message')
log.error('error message')
print('LOG FORMAT VERIFIED')
"</automated>
</verify>
<done>DEBUG/INFO/WARNING/ERROR 四级日志正常工作</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---------|-------------|
| Log output → stdout/file | No untrusted data crosses trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|---------|-----------|------------|------------|
| T-01-09 | Information Disclosure | log output | Accept | 日志为公开诊断信息 |
| T-01-10 | Denial | log file disk full | Accept | 操作系统管理 |
</threat_model>

<verification>
- DEBUG/INFO/WARNING/ERROR 四级正常工作
- 日志包含时间戳、模块名、级别
- Rich 彩色输出正常
</verification>

<success_criteria>
- 日志分级正常
- 格式包含时间戳/模块名/级别
- stdout 输出正常
</success_criteria>

<output>
创建 .planning/phases/01-项目基础设施/01-04-SUMMARY.md
</output>
