---
phase: "01-项目基础设施"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "pyproject.toml"
  - "uv.lock"
  - "src/facevidechange/__init__.py"
  - "src/facevidechange/__main__.py"
  - "src/facevidechange/cli.py"
  - "src/facevidechange/config.py"
  - "src/facevidechange/logging_.py"
  - "src/facevidechange/models/__init__.py"
  - "src/facevidechange/pipeline/__init__.py"
  - "src/facevidechange/io/__init__.py"
  - "config/presets.yaml"
autonomous: true
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "pyproject.toml defines all dependencies with locked versions"
    - "src/ 目录结构清晰，模块可导入"
    - "uv 可安装所有依赖"
  artifacts:
    - path: "pyproject.toml"
      provides: "项目依赖定义和入口点"
    - path: "src/facevidechange/__init__.py"
      provides: "包初始化"
    - path: "config/presets.yaml"
      provides: "内置预设配置"
  key_links:
    - from: "pyproject.toml"
      to: "src/facevidechange"
      via: "python package"
---

<objective>
建立 Python 项目基础结构：uv 依赖管理、pyproject.toml、目录骨架、模块初始化。

Purpose: 为后续所有计划提供基础结构
Output: 可安装的 Python 包
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-项目基础设施/01-CONTEXT.md
@.planning/phases/01-项目基础设施/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
<name>Task 1: 创建 pyproject.toml</name>
<files>pyproject.toml</files>
<read_first>pyproject.toml (new file)</read_first>
<action>
创建 pyproject.toml，内容如下：

```toml
[project]
name = "facevidechange"
version = "0.1.0"
description = "Real-time video stream face swapping CLI tool"
requires-python = ">=3.10"
dependencies = [
    "typer[cli]>=0.12.0",
    "rich>=13.0.0",
    "ruamel.yaml>=0.18.0",
    "opencv-python>=4.10.0",
    "onnxruntime-gpu>=1.18.0",
    "insightface>=0.7.0",
]

[project.scripts]
facevidechange = "facevidechange.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/facevidechange"]
```

所有版本使用 >= 以便兼容更新的次版本，锁定在 lock 文件中。
</action>
<verify>
<automated>grep -q "facevidechange\[cli\]" pyproject.toml && grep -q "typer\[cli\]" pyproject.toml && grep -q "rich>=" pyproject.toml</automated>
</verify>
<done>pyproject.toml 存在，包含 facevidechange 入口点定义</done>
</task>

<task type="auto">
<name>Task 2: 创建目录结构和空模块</name>
<files>src/facevidechange/__init__.py, src/facevidechange/__main__.py, src/facevidechange/models/__init__.py, src/facevidechange/pipeline/__init__.py, src/facevidechange/io/__init__.py</files>
<read_first>src/facevidechange/__init__.py (new file)</read_first>
<action>
创建以下目录和空模块文件:

1. `src/facevidechange/__init__.py` — 包初始化
```python
"""FaceVideoChange — Real-time video stream face swapping CLI tool."""
__version__ = "0.1.0"
```

2. `src/facevidechange/__main__.py` — 支持 `python -m facevidechange` 运行
```python
from facevidechange.cli import app
app()
```

3. `src/facevidechange/models/__init__.py`
```python
"""Face detection, embedding, and swapping models."""
```

4. `src/facevidechange/pipeline/__init__.py`
```python
"""Video processing pipeline."""
```

5. `src/facevidechange/io/__init__.py`
```python
"""Video capture and streaming I/O."""
```

使用 mkdir -p 创建所有目录，然后创建 __init__.py 文件。
</action>
<verify>
<automated>python -c "import facevidechange; print(facevidechange.__version__)"</automated>
</verify>
<done>facevidechange 包可导入，__version__ = "0.1.0"</done>
</task>

<task type="auto">
<name>Task 3: 创建内置预设配置</name>
<files>config/presets.yaml</files>
<read_first>config/presets.yaml (new file)</read_first>
<action>
创建 `config/presets.yaml`，包含内置预设配置（per D-08, D-09）：

```yaml
# FaceVideoChange 内置预设配置
# 使用 --preset <name> 加载

presets:
  realtime-8gb:
    description: "RTX 4060 实时换脸（推荐）"
    fps: 30
    resolution: [1280, 720]
    model: "inswapper_128_fp16"
    quality: 75
    face_swap_strength: 0.85
    stream:
      enable: true
      codec: "h264"
      bitrate: "2500k"
    record:
      enable: false

  quality-8gb:
    description: "RTX 4060 高质量模式"
    fps: 15
    resolution: [1920, 1080]
    model: "inswapper_128_fp16"
    quality: 90
    face_swap_strength: 1.0
    stream:
      enable: true
      codec: "h264"
      bitrate: "5000k"
    record:
      enable: false

  preview-only:
    description: "仅本地预览，无推流"
    fps: 30
    resolution: [1280, 720]
    model: "inswapper_128_fp16"
    quality: 75
    face_swap_strength: 0.85
    stream:
      enable: false
    record:
      enable: false
```

resolution 格式为 [宽度, 高度] 列表。
</action>
<verify>
<automated>grep -q "realtime-8gb" config/presets.yaml && grep -q "quality-8gb" config/presets.yaml && grep -q "preview-only" config/presets.yaml</automated>
</verify>
<done>config/presets.yaml 存在，包含 realtime-8gb、quality-8gb、preview-only 三个内置预设</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---------|-------------|
| CLI → config file | Untrusted config file from user home directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|---------|-----------|------------|------------|
| T-01-01 | Tampering | config/presets.yaml | Mitigate | YAML 文件仅用于预设，不执行代码 |
| T-01-02 | Denial | uv install | Accept | 网络安装失败是正常环境问题 |
</threat_model>

<verification>
- `python -c "import facevidechange; print(facevidechange.__version__)"` 执行成功
- `python -m facevidechange --help` 显示帮助信息（Plan 03 完成后）
- `grep -q "realtime-8gb" config/presets.yaml` 返回 0
</verification>

<success_criteria>
- facevidechange 包可被 Python 导入
- pyproject.toml 包含正确的入口点定义
- config/presets.yaml 包含至少 3 个内置预设
</success_criteria>

<output>
创建 .planning/phases/01-项目基础设施/01-01-SUMMARY.md
</output>
